<UserControl x:Class="MyApp.Desktop.Views.RestaurantView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyApp.Desktop.Views"
      xmlns:viewmodel="clr-namespace:MyApp.Shared.ViewModels;assembly=MyApp.Shared"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewmodel:RestaurantViewModel />
    </UserControl.DataContext>

    <StackPanel Margin="10">

        <Label>Étterem lista</Label>

        <ListView ItemsSource="{Binding Restaurants}"
                  SelectedItem="{Binding SelectedRestaurant, Mode=TwoWay}">

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Név" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Leírás" DisplayMemberBinding="{Binding Description}" />
                    <GridViewColumn Header="Helyszín" DisplayMemberBinding="{Binding Location}" />
                    <GridViewColumn Header="Értékelés" DisplayMemberBinding="{Binding AverageRating}" />
                </GridView>
            </ListView.View>

            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Restaurants.Count}" Value="0">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListView">
                                        <Grid>
                                            <TextBlock Text="Nincs megjeleníthető étterem."
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Opacity="0.6"/>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>

        </ListView>

        <Button Content="Törlés"
                Command="{Binding DeleteSelectedCommand}"
                Margin="0,10,0,0"/>
    </StackPanel>
</UserControl>
